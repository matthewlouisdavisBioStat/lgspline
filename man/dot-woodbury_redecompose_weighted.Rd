% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_B.R
\name{.woodbury_redecompose_weighted}
\alias{.woodbury_redecompose_weighted}
\title{Per-Iteration Weighted Woodbury Redecomposition}
\usage{
.woodbury_redecompose_weighted(
  Delta_V,
  X_block,
  DV_X,
  W,
  X,
  K,
  p_expansions,
  X_gram_weighted,
  Lambda,
  schur_corrections,
  unique_penalty_per_partition,
  L_partition_list,
  parallel_eigen,
  cl,
  chunk_size,
  num_chunks,
  rem_chunks,
  rank_threshold_fraction = 1/3,
  family
)
}
\arguments{
\item{Delta_V}{Fixed \eqn{N \times N} matrix
\eqn{\mathbf{V}^{-1} - \mathbf{I}}.}

\item{X_block}{Full \eqn{N \times P} block-diagonal design matrix.}

\item{DV_X}{Precomputed \eqn{N \times P} product
\eqn{\boldsymbol{\Delta}_V \mathbf{X}}.}

\item{W}{Length-\eqn{N} vector of current GLM working weights.}

\item{X}{List of partition-specific design matrices.}

\item{K, p_expansions}{Integer dimensions.}

\item{X_gram_weighted}{List of \eqn{K+1} weighted Gram matrices
\eqn{\mathbf{X}_k^{\top}\mathrm{diag}(\mathbf{W}_k)\mathbf{X}_k}.}

\item{Lambda}{Shared \eqn{p \times p} penalty matrix.}

\item{schur_corrections}{List of \eqn{K+1} Schur correction matrices.}

\item{unique_penalty_per_partition}{Logical.}

\item{L_partition_list}{Partition-specific penalty matrices.}

\item{parallel_eigen}{Logical; parallel eigendecomposition.}

\item{cl, chunk_size, num_chunks, rem_chunks}{Parallel parameters.}

\item{rank_threshold_fraction}{Numeric; Woodbury threshold (default 1/3).}

\item{family}{GLM family object.}
}
\value{
A list with the same structure as \code{.woodbury_decompose_V}:
\describe{
  \item{use_woodbury}{Logical; FALSE if rank exceeds threshold.}
  \item{Ghalf_corrected}{List of corrected \eqn{\mathbf{G}_{c,k}^{1/2}}.}
  \item{GhalfInv_corrected}{List of corrected \eqn{\mathbf{G}_{c,k}^{-1/2}}.}
  \item{G_corrected}{List of corrected \eqn{\mathbf{G}_{c,k}}.}
  \item{L}{Off-diagonal low-rank factor (\eqn{P \times r}).}
  \item{S_signs}{Length-\eqn{r} sign vector.}
  \item{r}{Integer effective rank.}
  \item{M}{Precomputed \eqn{r \times r} Woodbury inner inverse.}
  \item{GL}{List of per-partition \eqn{\mathbf{G}_{c,k}\mathbf{L}[rows_k,]}.}
}
}
\description{
Given the FIXED \eqn{\boldsymbol{\Delta}_V = \mathbf{V}^{-1} -
\mathbf{I}} and precomputed \eqn{\boldsymbol{\Delta}_V \mathbf{X}}
(both invariant across Newton iterations), together with the current
iteration's GLM working weights \eqn{\mathbf{W}} and weighted Gram
matrices, computes all Woodbury components needed for the constrained
step.
}
\details{
The weighted perturbation is decomposed as
\eqn{\boldsymbol{\Delta}(\mathbf{W}) = \mathbf{X}^{\top}
\mathrm{diag}(\mathbf{W})(\mathbf{V}^{-1} - \mathbf{I})\mathbf{X}},
split into block-diagonal corrections (absorbed into the corrected
Gram) and an off-diagonal remainder factored at low rank.
}
\keyword{internal}
